// Re-exported for import-path stability within this module; the class itself now lives in
// @network-crm/contracts so every services-or-modules/* package shares one definition
// (see packages/contracts/src/errors.ts).
export { DomainError } from "@network-crm/contracts";
