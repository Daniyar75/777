# services-or-modules/governance

AuditLog, AutomationDefinition, tenant-wide configuration versions. Bounded context: Governance (`docs/requirements/domain-model.md` §1). Added beyond the handoff's illustrative folder list so AuditLog has an explicit owner (see ADR-0010 §Decision). Communicates with other modules only via application ports/events, same rule as every other module (handoff §5).
