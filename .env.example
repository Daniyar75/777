# Copy to .env (git-ignored) and fill in for local development. apps/api/src/env.ts reads
# these from process.env; nothing here is a default baked into code.

DATABASE_URL=postgres://crm_app:crm_app_dev_pw@localhost:5432/network_crm_dev
JWT_SECRET=change-me-to-a-long-random-value
PLATFORM_PROVISIONING_KEY=change-me-guards-POST-/tenants-until-real-platform-owner-auth-exists
PORT=3000
