# packages/contracts

Shared DTO/event schemas consumed across modules (API request/response contracts, AsyncAPI-generated event types). Not domain entities — domain models stay inside their owning module. See `docs/api/openapi-skeleton.yaml` and `docs/events/asyncapi-skeleton.yaml`.
